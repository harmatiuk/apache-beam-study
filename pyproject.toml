[project]
name = "beam-study"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "apache-beam",
    "faker",
]

[tool.setuptools.packages.find]
where = ["src"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
